export interface User {
    username: string;
    cpassword: string;
  }

  export interface newUsers {
    id: number;
    name: string;
    phone: string;
    email: string;
    cpassword: string;
}